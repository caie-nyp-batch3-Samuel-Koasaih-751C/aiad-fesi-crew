# -----------------------------
# Core Python / Notebooks
# -----------------------------
ipython>=8.10.0
jupyterlab>=3.0
notebook
setuptools; python_version >= "3.12"

# -----------------------------
# Kedro
# -----------------------------
kedro[jupyter]==0.19.14
kedro-datasets[pandas]==7.0.0
kedro-datasets[pillow]>=7.0.0     # needed for kedro_datasets.pillow.ImageDataSet

# -----------------------------
# Core ML & Stats
# -----------------------------
numpy==1.26.4
pandas==1.5.3
scikit-learn==1.5.1
scipy==1.13.1
xgboost==2.0.3
imblearn==0.0

# -----------------------------
# Computer Vision stack
# -----------------------------
Pillow>=9.5
opencv-python-headless>=4.8       # headless: smaller + no GUI deps (good for Docker/K8s)
# If you use OpenCV locally with GUI viewers, add: opencv-python>=4.8

# Deep learning backbones (DenseNet/ResNet/EfficientNet via torchvision)
# Use a flexible lower bound to avoid Python version conflicts.
torch>=2.2
torchvision>=0.17
tensorflow==2.15.0

# Augmentations (optional but handy)
albumentations>=1.3

# -----------------------------
# Web service / API (choose Flask now; can add FastAPI later)
# -----------------------------
Flask==2.3.3     
Werkzeug>=3.0
gunicorn>=21.2               # production WSGI server for Flask in Docker/K8s
# FastAPI alternative (uncomment if you switch):
# fastapi>=0.111
# uvicorn[standard]>=0.30
# python-multipart>=0.0.9      # needed for file uploads in FastAPI

# -----------------------------
# Data access / Ops
# -----------------------------
python-dotenv>=1.0              # load .env for paths/keys
requests>=2.31
kaggle>=1.6                     # CLI to download datasets
boto3>=1.34                     # S3/MinIO if you use object storage
# minio>=7.2.5                 # alternative MinIO client
# kubernetes>=29.0             # if your runner service submits K8s Jobs from Python
gdown>=5.1

# -----------------------------
# Visualization
# -----------------------------
matplotlib==3.7.5
seaborn==0.12.2

# -----------------------------
# SQL / Database
# -----------------------------
sqlalchemy==1.4.46
sqlite-utils>=3.31
tables==3.10.1

# -----------------------------
# Kedro Viz (optional)
# -----------------------------
kedro-viz>=6.7.0

# -----------------------------
# Dev tooling (keep out of prod images if you can)
# -----------------------------
pytest>=7.4
ruff>=0.5
#poetry>=1.7          # normally installed outside requirements; leave here only if you insist